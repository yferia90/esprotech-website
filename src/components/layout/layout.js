import React from 'react';
import Footer from "../footer";

const Layout = ({ children }) => {
    <div>
        ROlando
        <main>
            {children}
        </main>
        <Footer/>
    </div>
}

export default Layout;